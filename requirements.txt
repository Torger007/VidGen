# VidGen requirements
# Python: 3.11+
#
# Install:
#   pip install -r requirements.txt
#
# Notes:
# - Base dependencies are enough for local eager + mock pipeline mode.
# - Celery mode needs a running Redis service.
# - ML dependencies are optional for real model inference.

# ---- Base runtime ----
celery>=5.4.0
fastapi>=0.115.0
numpy>=2.1.0
pillow>=11.0.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12
redis>=5.2.0
sqlmodel>=0.0.22
structlog>=24.4.0
uvicorn[standard]>=0.32.0

# ---- Dev / test ----
httpx>=0.28.0
pytest>=8.3.0
ruff>=0.7.0

# ---- Optional ML stack (real generation path) ----
accelerate>=1.1.0
controlnet-aux>=0.0.9
diffusers>=0.31.0
imageio[ffmpeg]>=2.36.0
peft>=0.13.0
safetensors>=0.4.5
torch>=2.5.0
transformers>=4.46.0
